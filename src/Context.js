import { createContext } from "react";

const UserContext = createContext(null);

const Popups = createContext(null);

const CreateTaskContext = createContext(null);

export { UserContext, Popups, CreateTaskContext };